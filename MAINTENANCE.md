# ClipTiny 维护手册

## 1. 产品边界

ClipTiny 是一个管理文本和图片的原生 macOS 菜单栏应用：

- 后台轮询系统剪贴板，文本和图片合计保留条目数可配置为 50、100、200 或 500。
- `⌘⇧V` 是窗口开关：隐藏时打开，显示时关闭。
- `⌘F` 聚焦搜索框。
- `↑ / ↓` 选择记录，右侧预览区同步显示完整文本或整张图片。
- `Enter` 将记录写回剪贴板、关闭窗口，并在有文本光标时粘贴到之前的应用。
- `Esc` 隐藏窗口。
- `⌘W` 隐藏窗口。
- 窗口可以调整大小，关闭或隐藏时保存位置和大小；若显示器布局变化导致旧位置
  不可见，则居中显示。
- 无网络请求、无第三方依赖、无 Dock 图标。

当前最低系统版本为 macOS 13，支持 Apple 芯片和 Intel Mac。

## 2. 交互状态与流程

### 全局快捷键

`GlobalHotKey` 使用 Carbon 的 `RegisterEventHotKey` 注册 `⌘⇧V`，因此“打开历史”
本身不需要辅助功能权限。

快捷键回调调用 `HistoryWindowController.toggle()`：

1. `window.isVisible == false`：调用 `show()`。
2. `window.isVisible == true`：调用 `hide()`。

不要把快捷键回调直接改回 `show()`，否则会破坏再次按键关闭的行为。

### 打开窗口

`show()` 会：

1. 记录当前最前方应用，供稍后恢复和粘贴。
2. 刷新列表与搜索结果。
3. 激活 ClipTiny 并显示浮动窗口。
4. 有记录时选中第一条并聚焦列表；无记录时聚焦搜索框。

窗口位置只在控制器初始化时恢复，后续打开不会重新居中。恢复时会把宽高抬到
`HistoryWindowController.minimumSize`，避免预览区被挤没。

### 关闭窗口

`hide()` 会：

1. 保存当前窗口位置和大小。
2. 隐藏窗口。
3. 清空本次搜索词并恢复完整列表。
4. 重新激活打开 ClipTiny 前使用的应用。

窗口左上角关闭按钮通过 `windowWillClose` 保存位置。`Esc` 由
`ShortcutPanel.cancelOperation` 转发到 `hide()`；`⌘W` 由
`ShortcutPanel.performKeyEquivalent` 捕获后调用同一个 `hide()`。

### 搜索

`ShortcutPanel.performKeyEquivalent` 捕获 `⌘F`，并让搜索框成为第一响应者。
搜索使用 `localizedCaseInsensitiveContains`，不区分大小写，并保留第一项选择。
图片记录用一句描述（例如 `图片 2560 × 1440`）参与搜索。

`refresh()` 会记住选中记录的 `id` 并在 `reloadData()` 之后按 `id` 恢复选择，
这样窗口开着时复制了新内容，用户正在看的那条不会被顶掉。

### 预览

窗口右侧是常驻预览区，跟随列表选中项刷新：

- 文本记录：只读的 `NSTextView`，可滚动、可选中复制。
- 图片记录：解密原图后按比例缩小显示，不放大小图。
- 图片文件缺失时显示“图片文件已丢失，无法预览”，不影响其他记录。

列表每行左侧是 40pt 图标：图片记录用缩略图，文本记录用系统符号。

### 时间戳

每条记录都显示 `createdAt` 的绝对时间：

- 列表：`2026-07-30 19:17 · 大小`，精确到分钟。
- 预览区：`2026-07-30 19:17:02 · 3 分钟前 · 大小`，精确到秒，后面跟相对时间。

格式串固定为 `yyyy-MM-dd HH:mm`，用 `en_US_POSIX` 区域，避免跟随系统偏好变成
12 小时制。相对时间在一分钟以内显示“刚刚”，否则 `RelativeDateTimeFormatter`
会把刚复制的记录说成“0秒后”。

### 选择与粘贴

列表双击或按 `Enter` 后：

1. 用所选记录替换系统剪贴板：文本写 `.string`；图片写 `.png`，并附带 `.tiff`
   以兼容只认 TIFF 的旧应用。
2. 通过 `onPasteboardWrite` 把新的 `changeCount` 交给 `ClipboardMonitor`，
   这次写入不会被再记一遍，历史顺序保持不变。
3. 调用 `hide()`：保存窗口位置、关闭窗口、清空搜索并激活之前的应用。
4. 检查辅助功能权限；已授权时直接使用，未授权且尚未提示过时只请求一次。
5. 有权限时，检查系统级焦点元素是否能接受文本输入（`AXValue` 可写、
   有 `AXSelectedTextRange`，或角色是 `AXTextField` / `AXTextArea` /
   `AXComboBox`）。目标应用刚被激活时焦点可能还没就绪，所以最多重试 10 次，
   间隔 0.12 秒。
6. 明确是非文本控件时不粘贴；部分应用不暴露完整 AX 信息时，目标应用回到前台后
   最后兜底发送一次 `⌘V`。
7. 无辅助功能权限时只完成复制；首次自动粘贴会请求一次授权，拒绝后不重复弹窗，
   用户可在“系统设置 → 隐私与安全性 → 辅助功能”中手动授权。

读取剪贴板、搜索、预览以及全局快捷键都不依赖辅助功能权限。

## 3. 数据、隐私与持久化

### 历史记录

数据文件：

```text
~/Library/Application Support/ClipTiny/history.enc
~/Library/Application Support/ClipTiny/images/<记录 id>.img
~/Library/Application Support/ClipTiny/images/<记录 id>.thumb
```

规则：

- 启动时从 macOS 钥匙串读取 256 位随机密钥。
- 历史先编码为 JSON，再使用 CryptoKit AES-GCM 加密；磁盘上没有明文历史。
- 图片不进 JSON。原图和缩略图各自单独用同一把密钥加密成 `.img` 和 `.thumb`，
  JSON 里只留尺寸、字节数和 SHA-256 摘要。
- 密钥保存在当前用户的 macOS 登录钥匙串，不写入项目目录或历史文件。
- 首次创建和同一构建的日常访问通常无提示。由于当前使用 ad-hoc 签名，应用升级
  后代码身份会变化，macOS 可能要求确认钥匙串访问；选择“始终允许”即可。
- 数据目录和 `images/` 权限固定为 `700`，加密文件权限固定为 `600`。
- 钥匙串不可用时采用失败关闭策略：文本仍可在本次运行中使用但不写盘；图片直接
  不保存，绝不会留下明文图片文件。
- 重复文本会删除旧项并移动到列表顶部；重复图片按 PNG 的 SHA-256 摘要判断，
  只把已有记录移到顶部，不再复制一份文件。
- 空字符串不保存，但不会擅自裁剪空格和换行。
- 写入后只保留配置数量内的最新记录，被挤掉的图片记录会同时删除对应文件。
- 保留条目数通过菜单栏的“保留条目数”配置，可选 50、100、200 或 500，默认 100；
  配置保存在 UserDefaults 的 `MaximumHistoryItemCount`。调小上限会立即裁剪历史。
- “清空历史”会删除全部图片文件；启动加载成功后还会清理没有对应记录的孤儿文件。
- 加密后原子写入，应用退出后记录仍存在。
- 解码历史时缺少 `kind` 字段的记录按文本处理，图片相关字段允许缺省。

### 图片规则

- 识别的剪贴板类型：`public.png`、`public.tiff`、`public.jpeg`、
  `com.compuserve.gif`。
- 一律转成 PNG 保存，因此 GIF 只保留第一帧，动图会失去动画。
- 缩略图最长边 128 像素，列表用 `NSCache` 缓存最近 60 张，避免滚动时反复解密。
- 单张图片上限：源数据 64 MB、转换后的 PNG 20 MB，超过就跳过不记录。
- 剪贴板同时有图片和文字时的取舍在 `ClipboardImage.prefersImage(in:)`：
  没有文字，或文字只是图片地址（`http://`、`https://`、`file://`、`data:image/`）
  或图片文件名（`.png`、`.jpg` 等后缀）时按图片处理；其余按文字处理，避免带图
  富文本被记成一张图。判断不能用“有没有空格”，中文正文本来就没有空格。

### 忽略的内容

剪贴板包含以下类型时会被忽略：

```text
org.nspasteboard.TransientType
org.nspasteboard.ConcealedType
org.nspasteboard.AutoGeneratedType
```

这能避开部分密码管理器和应用明确标记的敏感或临时内容，但无法保证识别所有
第三方应用的敏感数据格式。

### 窗口位置

窗口位置存入 `UserDefaults`，键名为：

```text
HistoryWindowFrame
```

恢复时会检查保存的矩形是否与任一屏幕的 `visibleFrame` 相交，避免窗口落在
已断开的外接显示器上。

### Bundle Identifier

当前标识：

```text
com.local.ClipTiny
```

修改此标识会改变 UserDefaults、辅助功能授权和系统识别的应用身份。正式发布前
应换成稳定且属于开发者的反向域名标识，之后不要随意更改。

## 4. 源码结构

```text
ClipTiny/
├── Package.swift
├── Resources/
│   ├── AppIcon.icns
│   └── Info.plist
├── Sources/ClipTiny/
│   ├── main.swift
│   ├── AppDelegate.swift
│   ├── ClipboardMonitor.swift
│   ├── ClipboardImage.swift
│   ├── GlobalHotKey.swift
│   ├── KeychainKeyStore.swift
│   ├── HistoryStore.swift
│   ├── ImageVault.swift
│   └── HistoryWindowController.swift
├── scripts/
│   ├── build-app.sh
│   ├── make-icon.sh
│   └── MakeIcon.swift
├── README.md
└── MAINTENANCE.md
```

职责：

- `main.swift`：创建并运行 `NSApplication`。
- `AppDelegate.swift`：组装组件、菜单栏图标、菜单命令和应用生命周期，
  并把窗口的剪贴板写入事件转给监听器。
- `ClipboardMonitor.swift`：每 0.35 秒检查 `changeCount`，有变化时读取图片或
  文本；`acknowledgeOwnWrite(changeCount:)` 用来跳过应用自己的写入。
- `ClipboardImage.swift`：判断该按图片还是文字处理，把剪贴板图片转成 PNG 和
  缩略图，并算出去重用的摘要。
- `GlobalHotKey.swift`：注册和释放全局 `⌘⇧V`。
- `KeychainKeyStore.swift`：创建或读取仅限本机的历史加密密钥。
- `HistoryStore.swift`：数据模型、去重、可配置条目上限、AES-GCM 加解密、原子写入
  与清空。
- `ImageVault.swift`：图片文件的加密读写、缩略图缓存、删除与孤儿清理。
- `HistoryWindowController.swift`：窗口、列表、搜索、预览区、位置记忆、
  文本光标检测和自动粘贴。
- `Info.plist`：应用标识、版本、最低系统版本、`CFBundleIconFile` 以及
  `LSUIElement`。
- `MakeIcon.swift` / `make-icon.sh`：生成 `Resources/AppIcon.icns`。

### 应用图标

图标是代码画出来的，仓库里不存 PNG 素材：

- `MakeIcon.swift` 在 1024×1024 的坐标系里描述图形，十个尺寸各自按比例重新
  矢量绘制，比从大图缩小清楚。
- `make-icon.sh` 编译它、输出 `.iconset`，再用 `iconutil` 打成
  `Resources/AppIcon.icns`。
- 只有改了图形才需要重新生成，日常构建不跑：

```sh
chmod +x scripts/make-icon.sh
./scripts/make-icon.sh
```

- `build-app.sh` 把 `AppIcon.icns` 复制进 `Contents/Resources/`，并在签名后
  `touch` 一下 `.app`，否则 Finder 会继续显示缓存的旧图标。
- `Info.plist` 里的 `CFBundleIconFile` 写的是不带后缀的 `AppIcon`。
- 菜单栏图标是另一回事，用的是 SF Symbol `clipboard`（见 `AppDelegate`），
  菜单栏需要单色模板图，不要换成这个彩色图标。
- 因为 `LSUIElement` 为真，应用没有 Dock 图标，这个图标只在 Finder、
  登录项列表这类地方出现。

## 5. 本地开发与构建

### 环境

- macOS
- Apple Command Line Tools 或完整 Xcode
- Swift 5.9 兼容工具链
- 不需要下载依赖

构建脚本会优先使用：

```text
/Library/Developer/CommandLineTools/SDKs/MacOSX15.4.sdk
```

该 SDK 不存在时，脚本会自动使用 `xcrun --sdk macosx --show-sdk-path` 返回的
当前 SDK。

### 构建通用应用

在项目根目录运行：

```sh
chmod +x scripts/build-app.sh
./scripts/build-app.sh
```

脚本分别编译 `x86_64` 和 `arm64`，再通过 `lipo` 合并为通用二进制，并进行
ad-hoc 本地签名。输出位置：

```text
outputs/ClipTiny.app
```

编译缓存和中间二进制位于 `work/`，可安全删除并重新生成。

### 验证

每次发布至少运行：

```sh
codesign --verify --deep --strict --verbose=2 outputs/ClipTiny.app
lipo -archs outputs/ClipTiny.app/Contents/MacOS/ClipTiny
plutil -lint outputs/ClipTiny.app/Contents/Info.plist
outputs/ClipTiny.app/Contents/MacOS/ClipTiny --verify-keychain
```

第二条命令应输出：

```text
x86_64 arm64
```

### 压缩发布包

不要用普通文件复制破坏 `.app` 结构。使用：

```sh
ditto -c -k --keepParent \
  outputs/ClipTiny.app \
  outputs/ClipTiny-universal.zip

unzip -tq outputs/ClipTiny-universal.zip
shasum -a 256 outputs/ClipTiny-universal.zip
```

打包前同步更新 `Resources/Info.plist` 中：

- `CFBundleShortVersionString`
- `CFBundleVersion`

当前构建是 ad-hoc 签名，适合本机使用和内部测试。对外分发时应使用 Apple
Developer ID 签名并完成 notarization，否则 Gatekeeper 可能警告或阻止打开。

## 6. 回归测试清单

每次改动交互或打包后手动验证：

1. 启动后只出现菜单栏图标，没有 Dock 图标。
2. 复制新文本后，`⌘⇧V` 能看到记录。
3. 连续复制相同文本时只保留一条，并移动到顶部。
4. 截图或复制图片后出现图片记录，列表有缩略图，右侧能看到整张图。
5. 重复复制同一张图只保留一条，`images/` 里不会多出文件。
6. 选中图片按 `Enter`，能粘贴进支持图片的应用（备忘录、聊天窗口等）。
7. 菜单栏可切换保留 50、100、200 或 500 条；调小上限后多出的最旧记录及其
   图片文件立即被删除，重启后配置仍然生效。
8. `⌘⇧V` 第一次打开、第二次关闭、第三次仍能打开。
9. 移动或缩放窗口后用快捷键关闭，再打开时位置和大小保持不变。
10. `⌘F` 能从列表聚焦搜索框，中文和英文搜索均可用。
11. `↑ / ↓`、`Enter`、双击、`Esc` 和 `⌘W` 行为正确，预览随选中项变化。
12. 光标停在输入框时按 `Enter` 会自动粘贴，窗口同时关闭。
13. 光标不在输入框（例如停在 Finder）时按 `Enter` 不粘贴，之后手动 `⌘V`
    能拿到内容。
14. 粘贴之后历史顺序不变，不会多出一条重复记录。
15. 无辅助功能权限时仍会复制所选内容，不崩溃。
16. 退出并重启后历史和图片仍存在。
17. `history.enc` 中搜索不到任何已复制的明文，`images/` 下的文件不是可打开的
    图片，权限均为 `600`。
18. 钥匙串包含 ClipTiny 的密钥，正常启动和读取不会要求用户输入密码。
19. 外接显示器断开后，窗口不会恢复到不可见区域。
20. 菜单中的“清空历史”会同时清掉 `images/` 下的文件；“退出”可用。
21. Finder 里 `outputs/ClipTiny.app` 显示为“应用程序”并带自定义图标，
    `Contents/Resources/AppIcon.icns` 存在。
22. Apple 芯片和 Intel 架构均存在，签名与压缩包校验通过。

## 7. 常见修改注意点

- 调整历史上限：修改 `HistoryStore.maximumCount`，并同步更新界面文案和文档。
- 调整图片体积上限：`HistoryStore.maximumImageBytes` 和
  `ClipboardImage.maximumSourceBytes` 要一起看，前者是入库上限，后者是解码前
  的保护。
- 调整缩略图尺寸：修改 `ClipboardImage.thumbnailMaxSide`；已有记录的缩略图不会
  重新生成。
- 调整轮询性能：修改 `ClipboardMonitor.start()` 的时间间隔和 `tolerance`；过短会
  增加唤醒次数，过长会让记录出现延迟。
- 修改快捷键：同步修改 `GlobalHotKey`、菜单标题、README 和本手册。
- 增加文件或富文本支持：需要重做数据模型和隐私策略，走 `ImageVault` 那种
  “大对象单独加密存盘、JSON 只留元数据”的思路，不要把大对象塞进 JSON。
- 修改自动粘贴判定：`focusedElementAcceptsText()` 放宽会让 ClipTiny 在非输入区
  也发 `⌘V`（例如在 Finder 里触发意外操作），收紧则会在一些不暴露辅助功能信息
  的应用里失去自动粘贴，只能手动 `⌘V`。
- 修改窗口显示策略：必须回归测试快捷键开关、前台应用恢复和自动粘贴目标。
- 修改 Bundle Identifier 或签名身份后，辅助功能权限通常需要重新授权。

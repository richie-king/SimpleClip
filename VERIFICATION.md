# 验证记录（2026-09-16）

## 结果

- 核心自动化测试：26/26 通过。
- AppKit UI 集成测试：6/6 通过。
- 真实桌面场景：7/7 已执行场景通过；授权后的自动粘贴路径跳过。
- 打包检查：通过。

## 真实桌面验证

使用同一份生产 AppDelegate、窗口、剪贴板监控和全局快捷键代码，注入隔离历史，
通过 CGEvent 发送系统级按键。跨应用接收方为独立 AppKit 富文本接收器。
测试结果不代表所有第三方应用兼容性。

1. 启动后为 accessory 菜单栏模式，历史窗口隐藏。
2. ⌘⇧V 连续打开、关闭、再次打开。
3. 无辅助功能授权时 Enter 只复制并恢复原应用；手动 ⌘V 成功，历史不重复。
4. ⌘C 只复制并关闭；手动 ⌘V 成功。
5. 图片复制提供 PNG/TIFF，在接收器中手动粘贴为真实图片附件，历史不重复。
6. ⌘F 后真实键盘输入搜索；第一次 Esc 清空，第二次关闭。
7. 测试进程退出后重新启动，从磁盘恢复文本和图片记录。读取的是重启后新生成的状态。

原应用已恢复运行（`/Applications/ClipTiny.app`），测试前的剪贴板内容已由驱动恢复。
JSON 结果：`work/desktop-tests/run.IA1PPr/results.json`。

## 打包验证

本轮重新构建 `outputs/ClipTiny.app`，版本 2.0.4（9）：

- x86_64 + arm64；两个架构的最低系统版本均为 26.0，SDK 26.5。
- Info.plist 合法，APPL/LSUIElement 标记正确，二进制可执行。
- 图标和 Info.plist 与源资源一致；图标为 1024 × 1024 ICNS。
- ad-hoc 签名严格校验通过。
- ZIP 校验通过；解压后签名、可执行权限与二进制一致性通过。

校验包：`work/verification/ClipTiny-verified.zip`。
SHA-256：`ddbb3aab791d71d00ab18ba41523fd828adfe479da1eca1d8c332ce1ef078c2d`。
没有替换 `/Applications` 中的已安装版本。双架构验证包括构建和格式检查，
不代表已在 Apple 芯片硬件上执行测试；本机执行环境为 x86_64。

## 最终人工验收

- [ ] 为验收版本授权辅助功能后，Enter 在实际常用应用中自动粘贴文本和图片。
- [ ] 已授权时，Finder 等非文本焦点不会意外触发粘贴。
- [ ] 多桌面 Spaces 切换、外接显示器断开后的窗口恢复。
- [ ] 浅深色、减少透明度/增强对比度、长文本与图片的视觉效果。
- [ ] 实际安装版本的钥匙串首次授权、升级后授权体验。
- [ ] 若用于公开分发，另行验证 Developer ID 签名和公证；本轮仅验证本地 ad-hoc 签名。

自动粘贴未执行的原因：桌面驱动具备权限，但隔离测试应用未获辅助功能授权。
驱动因此验证无授权降级路径，并在报告中明确跳过授权路径。

## 复跑

```sh
CLIPTINY_UI_TESTS=1 ./scripts/test.sh
./scripts/test-desktop.sh
./scripts/build-app.sh
./scripts/verify-app.sh
```

桌面测试会短暂接管焦点，期间不要使用键盘或复制内容。详细说明见 MAINTENANCE.md。
